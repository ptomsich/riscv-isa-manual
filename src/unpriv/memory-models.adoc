[[mm]]
== RISC-V Memory Models

NOTE: This chapter is currently being restructured.
Its contents are normative, but the presentation might appear disjoint.

This chapter describes the two RISC-V memory consistency models:
<<memorymodel,RVWMO>>, the base weakly ordered model, and <<ext:ztso,RVTSO>>,
a more strongly ordered model enabled via the ext:ztso[] standard extension.

Additional explanatory material for both models can be found in
<<app:mm-explanatory>>.

NOTE: On implementations whose native model is RVWMO, the supervisor-level
ext:ssdtso[] extension (<<ext:ssdtso>>) lets privileged software enable RVTSO
dynamically — per hart and per privilege level — for software running at lower
privilege levels. It defines no new ordering rules: a hart in dynamic-TSO mode
applies exactly the annotation assignment of <<ext:ztso,RVTSO>>.

include::rvwmo.adoc[]
include::ztso.adoc[]
